services:
  web:
    # Docker Compose reads the exported $ECR_REPO_URL variable from the pipeline
    image: ${ECR_REPO_URL}:latest
    ports:
      - "80:80"
    env_file:
      - .env.dev
    restart: always
