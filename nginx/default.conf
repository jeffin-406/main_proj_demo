server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        # This line is critical for React Router (Single Page App)
        try_files $uri $uri/ /index.html;
    }
