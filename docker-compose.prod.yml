services:
  web:
    # Uses the specific deployment tag generated by the workflow
    image: ${ECR_REPO_URL}:${IMAGE_TAG}
    ports:
      - "8040:80"
    env_file:
      - .env.prod
    restart: always
